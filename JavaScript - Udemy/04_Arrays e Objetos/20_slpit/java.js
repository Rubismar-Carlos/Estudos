let frase = 'Testando o modo split';

let separandoFrase = frase.split(' ');

console.log(separandoFrase);

let produtos = 'Banana;Maça;Pêra;Caju;Abacaxi;Acerola';

let separandoProdutos = produtos.split(';');

console.log(separandoProdutos);

//split e para separar tudo dentro de uma variavel em array 

//para separar tem que ser colocado dentro das string EX: produtos.split('separador');

//frase.split(' '); Ex : frase.split( Mostrando que sera separado com espaço entre a string ' ' se colocar tudo junto ele separa cada letra);

//produtos.split(';'); Ex: produtos.split( mostrando que o ; e o separador entao temos que colocar dentro das string ';');